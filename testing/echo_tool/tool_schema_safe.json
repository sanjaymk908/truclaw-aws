[
  {
    "name": "read",
    "description": "Throwaway smoke-test tool, safe path (Track A). Named 'read' deliberately -- already in TruClaw-Policies.template.json's default safeTools, so no policy edit is needed. Echoes back whatever message it's given.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "message": {"type": "string", "description": "Any text to echo back."}
      },
      "required": ["message"]
    }
  }
]
