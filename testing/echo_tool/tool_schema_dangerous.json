[
  {
    "name": "send_email",
    "description": "Throwaway smoke-test tool, dangerous path (Track B). Named 'send_email' deliberately -- already in TruClaw-Policies.template.json's default alwaysDangerousTools, so no policy edit is needed. Doesn't actually send email; same echo behavior as the safe-path tool.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "message": {"type": "string", "description": "Any text to echo back."}
      },
      "required": ["message"]
    }
  }
]
